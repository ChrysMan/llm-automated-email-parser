name: torch312
channels:
  - defaults
dependencies:
  - python=3.12
  - pytorch==2.5.0
  - torchvision==0.20.0
  - torchaudio==2.5.0
  - pytorch-cuda=12.4
  - nvidia/label/cuda-12.4.0::cuda-toolkit
  - pip
  - pip:
      - -r file:requirements.txt
prefix: /home/chryssida/miniconda3/envs/torch312
